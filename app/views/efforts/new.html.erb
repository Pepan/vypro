<h1><%= t("page.title.efforts.new") %></h1>

<%= render 'form' %>

<section class="actions">
	<%= link_to t("page.navig.efforts.back"), project_task_path(@effort.task.project_id, @effort.task_id) %>
</section>

