<h1><%= t("page.title.annexes.edit") %></h1>

<%= render 'form' %>

<section class="actions">
	<%= link_to t("page.navig.annexes.back"), annexes_path(@annex.id) %> |
	<%= link_to t("app.helpers.show"), annex_path(@annex.id) %>
</section>