<h1><%= t('page.title.annexes.new') %></h1>

<%= render 'form' %>

<section class="actions">
	<%= link_to t('page.navig.annexes.back'), project_task_path(@annex.task.project_id, @annex.task_id) %>
</section>

