<h1><%= t("page.title.companies.edit") %></h1>

<%= render 'form' %>

<section class="actions">
	<%= link_to t("page.navig.companies.back"), companies_path(@company.id) %> |
	<%= link_to t("app.helpers.show"), company_path(@company.id) %>
</section>