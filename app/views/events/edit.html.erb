<h1><%= t("page.title.events.edit") %></h1>

<%= render 'form' %>

<section class="actions">
	<%= link_to t("page.navig.events.back"), events_path(@event.id) %> |
	<%= link_to t("app.helpers.show"), event_path(@event.id) %>
</section>